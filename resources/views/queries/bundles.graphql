{
    amMostviewedBundlePacks (productId: {{$product_id}}) {
        items {
            block_title,
            discount_type,
            discount_amount,
            apply_for_parent,
            cart_message,
            items {
                product {
                    sku
                    __typename
                    id
                    name
                    canonical_url
                    thumbnail {
                        url
                    },
                    price_range {
                        maximum_price {
                            regular_price {
                                currency,
                                value
                            }
                        }
                    }
                },
                qty,
                discount_amount
            }
        }
    }
}
